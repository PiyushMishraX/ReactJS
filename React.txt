Core ConceptsComponents: Think of these as reusable building blocks. You build a "Button" or "Navbar" once and use it everywhere.JSX: A syntax that looks like HTML but lives inside JavaScript. It makes the code easier to read and write.Virtual DOM: Instead of refreshing the entire page when something changes, React updates a "virtual" copy first and then only changes the specific parts of the actual page that need it. This makes apps feel very fast.Modern Features (What’s New in 2026)React Compiler: A newer addition that automatically optimizes your code. It removes the need for manual performance tuning (like useMemo), making your app faster without extra effort.Server Components (RSC): These allow parts of your app to be rendered on the server rather than the user's browser. This reduces the amount of "code weight" sent to the user, leading to near-instant load times.Suspense: A way to handle "loading states" gracefully. It allows your app to show a fallback (like a loading spinner) while parts of the page are still fetching data.The EcosystemReact is rarely used alone. Most developers use it as part of a "stack":Frameworks: Next.js and Remix are the most common. They provide the "skeleton" (routing, SEO, and server logic) for React apps.Styling: Tailwind CSS is the industry standard for making React apps look good quickly.State Management: For keeping track of data across the whole app, tools like Zustand or TanStack Query are currently preferred over older, more complex options like Redux.


React -> .jsx The Standard. Use this for any file containing React components/HTML-like tags.


create react folder

older method -

npm create-react-app
in terminal

react ko as a framework use karo next etc ke sath

npm create vite 
vite is used to create folder of js, vanilla js , react , preact etc

cd react-first

npm run dev

use simple react snippet extension

icons-
npm install lucide-react@next


go to the folder ( create using vite then cd folder )
tailwind -
npm install tailwindcss @tailwindcss/vite

vite.config.js- add
import tailwindcss from '@tailwindcss/vite'

plugins: [react()], -> plugins: [react(),tailwindcss()],


index.css add -
@import "tailwindcss";

extension for ease-
tailwind css intellisense



Feature based vs atomic based structure - 
feature is creating components for section ( such as nav)

atomic based is micro managing where every part is sepearate from each other and ca be reused , such as buttons etc

WE should follow combination of it 


remix icon -
npm install @remixicon/react

import { RiHeartFill, RiAdminLine } from '@remixicon/react';
( in react file needin g the icons)

using -
const MyComponent = () => {
  return (
    <div>
      <RiHeartFill size={36} color="red" /> {/* Customize size and color */}
      <RiAdminLine className="my-icon" /> {/* Add custom class names */}
    </div>
  );
};


HOOKS-

useSate  - state manage
useEffect - on side by side
useref - to select dom element , instead of query selector, use ref take sreference
useCOntext - manage global content ( to access from anywhere)
useReducer -  which thing can be accessed where , whether user is logged in or not for a feature / page, local storage managing 

useMemo,useCallback -- optimization

use state - basic state
useCOntext- global state
useReducer - advanced global state



axios -
npm i axios
or
npm install axios

add 
import axios from 'axios' in the file if needed 

axios



React router dom -
install
npm i react-router-dom


we use brwser router which use history api and routing is on client side


